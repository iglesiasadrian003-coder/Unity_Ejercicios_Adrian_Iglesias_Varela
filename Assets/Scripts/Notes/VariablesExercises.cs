using UnityEngine;

public class VariablesExercises : MonoBehaviour
{


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

        //variable local
        /*float x,
              y,
              z;
        x = 2.0f;
        x = Mathf.Pow((x + x), 2);
        x = Mathf.Sqrt(x + Mathf.Sqrt(x) + 5);
        Debug.Log(x);

        //variable entera
        int a,
            b;
        //variable real
        float c,
              d;
        //variable carácter
        char e,
             f;
        //variable lógica (true/false)
        bool g;

        //variable válida, es un entero
        a = 20;
        //variable válida, es un entero
        b = 12;
        //variable válida, '0' es un carácter (no es un número, es '0' carácter)
        f = '0';
        //variable válida, es un entero y está dentro del rango (-2,147,483,648 a 2,147,483,647)
        b = 5500;
        //variable válida, 'f' es un carácter
        e = 'f';
        //variable no válida, "e" es un carácter, y "0" es un entero --> e = '0';
        e = 0;
        //variable no válida, "a" es un entero, y "12.56" es un real --> float  a; --> a = 12.56f;
        a = 12.56;
        //variable no válida, "f" es un variable carácter, y "g" es una variable lógica --> 
        f = g;
        //variable no válida, "g" es una variable lógica, y 'falso' ni siquiera es un literal válido en C# ('f') --> g = false;
        g = 'falso';
        //variable no válida, el literal verdadero no es válido en C# (true) --> g = true;
        g = verdadero;*/

        /*c = 3 + 2 * 4; --> c = 11;
          c = 11 + 4; --> c = 15;
          b = 15 - 3; --> b = 12;
          a = 12 * 15; --> a = 180;
          Debug.Log(180 + 12 + 15);*/

        /*int a, 
            b, 
            c;
        a = 3;
        b = 4;
        c = a + 2 * b;
        c = c + b;
        b = c - a;
        a = b * c;
        Debug.Log(a + b + c);*/

        /*y = 5 - 2; --> y = 3;
          x = 3 * 3 + 1; --> x = 10
          z = (10 > (3 + 5)) --> z = (10 > 8); --> z = true;
          resultXY = 10 + 3;
          Debug.Log("El valor de la z es: " + (10 > 8) + "y el valor de x e y es: " + 13 );*/

        /*int x, 
            y, 
            resultXY;
        bool z;
        x = 5;
        y = x - 2;
        x = y * y + 1;
        z = (x > (y + 5));
        resultXY = x + y;
        Debug.Log("El valor de la z es: " + z + "y el valor de x e y es: " + resultXY);*/

        /* a = 5;
           b = 5;
           Debug.Log(5 + 5);*/

        /*int a, b;
        a = 10;
        b = 5;
        a = b;
        b = a;
        Debug.Log(a + b);*/

    }

    // Update is called once per frame
    void Update()
    {

        //variable local
        float x;

    }
}
