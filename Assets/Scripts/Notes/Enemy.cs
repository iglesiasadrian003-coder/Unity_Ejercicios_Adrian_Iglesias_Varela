using UnityEngine;

public class Enemy : MonoBehaviour
{

    //Zona de variables globales
    //declarar una variable : ámbito + tipo + nombre + nombre
    //público --> "public" accesible desde cualquier script
    //privado --> "private" accesible solo desde este script
    //[SerializeField] --> hace que una variable privada sea visible en el inspector de Unity
    //public int Intnumber;
    //private int _intNumber;
    /*
     * akjfaskfj
     * afjasfkj
     */

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
